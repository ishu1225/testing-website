# Local dev: point at Flask on your machine.
VITE_API_BASE_URL=http://localhost:5000

# Kubernetes/Docker image build: leave empty so the browser calls same-origin /api/*
# (nginx in the frontend container proxies to http://backend-service:5000).
