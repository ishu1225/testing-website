version: "3"

services:

  backend:
    build: ./server
    ports:
      - "5000:5000"
    networks:
      default:
        aliases:
          - backend-service

  frontend:
    build: ./client

    ports:
      - "3000:80"

    depends_on:
      - backend